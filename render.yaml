services:
  - type: web
    name: two-person-chat
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: STORAGE_DIR
        value: /var/data
    disk:
      name: chat-storage
      mountPath: /var/data
      sizeGB: 1
